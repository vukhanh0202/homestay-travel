import React from 'react';
import PropTypes from 'prop-types';

NotFound.propTypes = {

};

function NotFound() {
    return (
        <div>
            Not found
        </div>
    );
}

export default NotFound;